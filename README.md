# PhishSafeguard

PhishSafeguard — Phishing URL Detection & Protection System

## Setup (local)
1. Clone repository
2. Copy `.env.example` to `.env` and fill values.
3. Run `composer install`
4. Import database (`phishing_db.sql`) into MySQL
5. Start XAMPP and open `http://localhost/phish_safeguard`

## .env.example
